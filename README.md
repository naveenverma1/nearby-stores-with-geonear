# nearby-stores-with-geonear

JWT (JSON web tokens) authentication (decode/encode) - Login middleware

Admin and user API

Locate nearby stores in a user-defined radius or area.

used Geonear Aggregation
